"use client";
import {useEffect,useState} from "react";
export default function VercelConnection(){const[state,setState]=useState<any>({connected:false});const[busy,setBusy]=useState(false);async function load(){const r=await fetch("/api/vercel/status");if(r.ok)setState(await r.json())}useEffect(()=>{void load()},[]);async function disconnect(){setBusy(true);await fetch("/api/vercel/disconnect",{method:"POST"});await load();setBusy(false)}return <section className="mt-4 rounded-2xl border border-white/10 bg-white/[0.03] p-6"><div className="flex items-center justify-between gap-4"><div><h2 className="font-semibold">Vercel</h2><p className="mt-1 text-sm text-zinc-500">{state.connected?"Connected — ready for deployments.":"Connect Vercel so approved projects can be deployed directly."}</p></div>{state.connected?<button onClick={disconnect} disabled={busy} className="rounded-xl border border-white/10 px-4 py-2 text-sm hover:bg-white/5">{busy?"Disconnecting…":"Disconnect"}</button>:<a href="/api/vercel/connect" className="rounded-xl bg-white px-4 py-2 text-sm font-medium text-black hover:bg-zinc-200">Connect Vercel</a>}</div></section>}