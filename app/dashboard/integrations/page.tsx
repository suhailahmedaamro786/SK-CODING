import Link from "next/link";
import { requireUser } from "@/lib/auth";
import GitHubConnection from "./GitHubConnection";
import VercelConnection from "./VercelConnection";
export default async function IntegrationsPage(){await requireUser();return <main className="min-h-screen px-6 py-10"><div className="mx-auto max-w-5xl"><Link href="/dashboard" className="text-sm text-zinc-500 hover:text-white">← Dashboard</Link><h1 className="mt-8 text-3xl font-bold">Integrations</h1><p className="mt-2 text-zinc-500">Connect GitHub and Vercel once. SK Builder keeps credentials server-side and uses them only for your projects.</p><GitHubConnection/><VercelConnection/><section className="mt-4 rounded-2xl border border-white/10 p-6 text-sm text-zinc-500"><strong className="text-zinc-300">Supabase:</strong> SK Builder uses the configured server-side Supabase project for project state, AI provider settings and deployment records. Run the latest migration in <code>supabase/migrations</code> before first use.</section></div></main>}