import "server-only";
import { decryptSecret } from "@/lib/secrets";
const GITHUB_API="https://api.github.com";
async function githubFetch<T>(path:string,token:string,init:RequestInit={}):Promise<T>{const r=await fetch(GITHUB_API+path,{...init,cache:"no-store",headers:{accept:"application/vnd.github+json",authorization:`Bearer ${token}`,"x-github-api-version":"2022-11-28",...(init.headers||{})}});const b=await r.json().catch(()=>({}));if(!r.ok){const e=new Error(b?.message||`GitHub request failed: ${r.status}`);(e as Error&{status?:number}).status=r.status;throw e}return b as T}
export async function githubUser(token:string){return githubFetch<{id:number;login:string;name?:string;email?:string;avatar_url?:string}>("/user",token)}
export async function createGithubRepository(token:string,name:string,description?:string){return githubFetch<{id:number;name:string;full_name:string;html_url:string;default_branch:string;owner:{login:string}}>("/user/repos",token,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name,description,private:true,auto_init:true})})}
export async function upsertGithubFile(token:string,fullName:string,path:string,content:string,message:string,branch="main"){const [owner,repo]=fullName.split("/");if(!owner||!repo)throw new Error("INVALID_GITHUB_REPOSITORY");const encoded=path.split("/").map(encodeURIComponent).join("/");const r=await fetch(`${GITHUB_API}/repos/${owner}/${repo}/contents/${encoded}?ref=${encodeURIComponent(branch)}`,{cache:"no-store",headers:{accept:"application/vnd.github+json",authorization:`Bearer ${token}`,"x-github-api-version":"2022-11-28"}});let sha:string|undefined;if(r.ok){const b=await r.json();if(!Array.isArray(b))sha=b.sha}else if(r.status!==404){const b=await r.json().catch(()=>({}));throw new Error(b?.message||`GitHub lookup failed: ${r.status}`)}return githubFetch<any>(`/repos/${owner}/${repo}/contents/${encoded}`,token,{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({message,content:Buffer.from(content,"utf8").toString("base64"),branch,...(sha?{sha}:{})})})}
export async function getStoredGithubToken(encrypted:string){return decryptSecret(encrypted)}
